# Deprecation policy

Public operation names, parameter names, unit suffixes, error codes, and protocol versions change only with an explicit migration note. The backend may be replaced without changing public domain-value semantics.
