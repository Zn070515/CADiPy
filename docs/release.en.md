# Release discipline

The version is sourced from `src/cadipy/__init__.py`. A release requires portable quality gates, the normal integration mode, the strict real-SOLIDWORKS gate when the configured environment is available, package-content inspection, and documentation build validation.
