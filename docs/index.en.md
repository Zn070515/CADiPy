# CADiPy documentation

The current implementation milestone establishes maintainable SOLIDWORKS agent-automation boundaries: the public control plane uses domain values and one OpSpec registry, the Python COM backend owns execution details, and a future C# Worker follows the same port.

Read the [usage guide](usage.en.md), [API](api/index.en.md), [protocol](protocol.en.md), [compatibility matrix](compatibility.en.md), and [migration record](migration.en.md).
