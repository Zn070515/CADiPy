# Migration record

CADiPy used a local mature automation worktree as migration input, preserving engineering discipline and contract-audit methods without inheriting its Git history or unrelated product history. The input was semantically audited with KEEP/ADAPT/REMOVE classifications; the formal design and implementation plan are in `docs/superpowers/`.
