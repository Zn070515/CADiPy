# Benchmarks

The current work scope prioritizes correctness, target safety, and real SOLIDWORKS postcondition verification. Any benchmark must state the SOLIDWORKS version, document scale, COM session state, whether rebuild/verification is included, and whether the result came from a real instance.
