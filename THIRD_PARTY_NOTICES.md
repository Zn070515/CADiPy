# Third-party notices

CADiPy does not distribute unrelated application-specific or vendored conversion trees.

- `pywin32` is an optional Windows dependency used by the Python SolidWorks COM backend.
- `mcp` is an optional dependency used only by the MCP adapter integration.
- Development-only tools are listed in `pyproject.toml` and are not runtime package contents.

Each dependency remains subject to its upstream license. The project’s own code is released under the MIT license in `LICENSE`.
